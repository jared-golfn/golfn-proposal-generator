'use client'

import { useState, useRef, useEffect } from 'react'
import { motion } from 'framer-motion'
import { Zap, Globe, ChevronDown, ChevronUp, ArrowDown } from 'lucide-react'
import type { PartnerData, PlatformKPI } from '@/lib/template-types'
import { images } from '@/lib/images'
import { BrandSpendInput } from './BrandSpendInput'

const defaultKPIs: PlatformKPI[] = [
  { label: 'Registered Golfers', value: '125,000+' },
  { label: 'Peak Monthly Active', value: '35,502' },
  { label: 'Under 34', value: '56%' },
  { label: 'Countries', value: '130+' },
  { label: 'Avg MoM Growth', value: '~53%', subtitle: 'Power user cohort, 10 months' },
]

const PITCH_SUBTITLE = '125,000+ verified golfers who actually buy premium gear. Build an owned audience, activate it, and turn it into revenue.'

const pitchPartnerLogos: { name: string; url: string; h: number }[] = [
  { name: 'L.A.B. Golf', url: 'https://www.golfn.com/LAB.png', h: 28 },
  { name: 'Cobra Puma Golf', url: 'https://www.golfn.com/cobra.webp', h: 28 },
  { name: 'Bettinardi', url: 'https://www.golfn.com/bettinardi.webp', h: 28 },
  { name: 'Finn', url: 'https://cdn.sanity.io/images/e3wja34v/production/e325118a1a6270a4e7887a895cbf469cd211d40b-131x55.svg', h: 32 },
  { name: 'Hyperice', url: 'https://cdn.sanity.io/images/e3wja34v/production/36b4396feef4359c78c827dad89e3bc2c42929b2-3840x2160.png', h: 140 },
]

const NO_INVERT_SLUGS = new Set(['zerorestriction'])

function PortfolioBracket({ brands, agencyLogoUrl, agencyName }: { brands: { brandName: string; brandLogoUrl?: string }[]; agencyLogoUrl?: string; agencyName?: string }) { const validBrands = brands.filter(b => b.brandLogoUrl); const n = validBrands.length; const containerRef = useRef<HTMLDivElement>(null); const pillRefs = useRef<(HTMLDivElement | null)[]>([]); const agencyRef = useRef<HTMLDivElement>(null); const [lines, setLines] = useState<{ pillCenters: number[]; barLeft: number; barRight: number; agencyCenter: number; containerWidth: number } | null>(null); useEffect(() => { function measure() { const container = containerRef.current; if (!container) return; const rect = container.getBoundingClientRect(); const centers: number[] = []; pillRefs.current.forEach((el) => { if (el) { const r = el.getBoundingClientRect(); centers.push(r.left + r.width / 2 - rect.left) } }); let ac = rect.width / 2; if (agencyRef.current) { const ar = agencyRef.current.getBoundingClientRect(); ac = ar.left + ar.width / 2 - rect.left }; if (centers.length > 0) { setLines({ pillCenters: centers, barLeft: centers[0], barRight: centers[centers.length - 1], agencyCenter: ac, containerWidth: rect.width }) } }; measure(); window.addEventListener('resize', measure); return () => window.removeEventListener('resize', measure) }, [n]); if (n === 0) return null; return (<div ref={containerRef} className="flex flex-col items-center relative" style={{ maxWidth: '620px' }}><div className="grid w-full gap-3" style={{ gridTemplateColumns: `repeat(${n}, 1fr)` }}>{validBrands.map((b, i) => (<div key={b.brandName} ref={(el) => { pillRefs.current[i] = el }} className="flex items-center justify-center h-14 px-3 rounded-lg bg-[#1a1f2e] border border-[#2a3347]"><img src={b.brandLogoUrl} alt={b.brandName} className="h-7 md:h-8 w-auto max-w-[160px] object-contain" style={{ filter: 'brightness(0) invert(1)', opacity: 0.85 }} /></div>))}</div>{lines && (<svg width={lines.containerWidth} height="28" className="block" style={{ overflow: 'visible' }}>{lines.pillCenters.map((cx, i) => (<line key={i} x1={cx} y1="0" x2={cx} y2="10" stroke="#2a3347" strokeWidth="1.5" />))}<line x1={lines.barLeft} y1="10" x2={lines.barRight} y2="10" stroke="#2a3347" strokeWidth="1.5" /><line x1={lines.agencyCenter} y1="10" x2={lines.agencyCenter} y2="28" stroke="#2a3347" strokeWidth="1.5" /></svg>)}{!lines && <div style={{ height: '28px' }} />}{agencyLogoUrl && (<div ref={agencyRef} className="flex items-center justify-center h-16 px-6 rounded-lg bg-[#1a1f2e]/70 border border-[#2a3347]/70"><img src={agencyLogoUrl} alt={agencyName || 'Agency'} className="h-9 md:h-10 w-auto max-w-[200px] object-contain" style={{ filter: 'brightness(0) invert(1)', opacity: 0.7 }} /></div>)}</div>) }

function PartnerLogo({ slug, logoUrl, name }: { slug: string; logoUrl: string; name: string }) {
  const noInvert = NO_INVERT_SLUGS.has(slug)
  if (noInvert) {
    return (
      <div className="flex items-center gap-3 md:gap-4 shrink-0">
        <span className="text-[#6b7280] text-sm md:text-base hidden sm:inline">Prepared for</span>
        <div className="bg-white/95 rounded-xl px-5 py-3">
          <img src={logoUrl} alt={name} className="h-14 md:h-20 w-auto object-contain" />
        </div>
      </div>
    )
  }
  return (
    <div className="flex items-center gap-2 md:gap-3 shrink-0">
      <span className="text-[#6b7280] text-sm md:text-base hidden sm:inline">Prepared for</span>
      <img src={logoUrl} alt={name} className="h-8 md:h-12 w-auto" style={{ filter: 'brightness(0) invert(1)', opacity: 0.85 }} />
    </div>
  )
}

export function S01_Hero({ partner, hideBrandInput }: { partner: PartnerData; hideBrandInput?: boolean }) { const [showMarkets, setShowMarkets] = useState(false); const isPitch = !!hideBrandInput; const subtitle = isPitch ? ((partner.slug !== 'template' && partner.heroSubtitle) ? partner.heroSubtitle : PITCH_SUBTITLE) : (partner.heroSubtitle || 'GolfN helps brands create awareness, identify real user interest, build qualified audience cohorts, and continue activating those users through measurable follow-on campaigns.'); const headline = partner.heroHeadline; const heroVideo = partner.heroVideoUrl; const markets = partner.marketReach; const marketTitle = partner.marketReachTitle || 'Market Reach by Country'; const isCustomMarkets = !!partner.marketReachTitle; const namedTotal = markets ? markets.reduce((s, m) => s + m.users, 0) : 0; const othersCount = isCustomMarkets ? 0 : (markets ? Math.max(0, 125000 - namedTotal) : 0); const grandTotal = namedTotal + othersCount; const showPartnerLogo = !partner.isPortfolio && partner.partnerLogoUrl && partner.campaigns && partner.campaigns.length > 0; const hasVideo = !!heroVideo; const showBrandInput = hasVideo && !hideBrandInput; return ( <section className="relative min-h-[85vh] flex flex-col justify-center overflow-hidden"> {hasVideo && (<><video autoPlay muted loop playsInline preload="auto" className="absolute inset-0 w-full h-full object-cover" style={{ zIndex: 0 }}><source src={heroVideo} type="video/mp4" /></video><div className="absolute inset-0 bg-[#0f1217]/80" style={{ zIndex: 1 }} /><div className="absolute inset-0 opacity-[0.08]" style={{ zIndex: 1, background: 'radial-gradient(ellipse 80% 60% at 50% 60%, #00ff9d, transparent)' }} /></>)} {!hasVideo && (<><div className="absolute inset-0 opacity-[0.07]" style={{ background: 'radial-gradient(ellipse 60% 50% at 15% 50%, #001a14, transparent)' }} /><div className="absolute inset-0 opacity-[0.04]" style={{ background: 'radial-gradient(ellipse 40% 40% at 85% 30%, #00ff9d, transparent)' }} /></>)} <div className="relative max-w-7xl mx-auto w-full px-6 md:px-12 py-12 md:py-16" style={{ zIndex: 10 }}> <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 0.1 }} className="flex items-center justify-between mb-12 md:mb-20 gap-4"> <img src={images.logo} alt="GolfN" className={`w-auto shrink-0 ${isPitch ? 'h-10 md:h-14' : 'h-8 md:h-12'}`} /> {partner.agencyLogoUrl ? (<div className="flex items-center gap-2 md:gap-3 shrink-0"><span className="text-[#6b7280] text-sm md:text-base hidden sm:inline">Prepared for</span><img src={partner.agencyLogoUrl} alt={partner.agencyName || ''} className="h-6 md:h-9 w-auto" style={{ filter: 'brightness(0) invert(1)', opacity: 0.85 }} /></div>) : partner.partnerLogoUrl ? (<PartnerLogo slug={partner.slug} logoUrl={partner.partnerLogoUrl} name={partner.partnerName} />) : !hasVideo ? (<span className="text-[#6b7280] text-sm md:text-base">Prepared for <span className="text-white font-medium">{partner.partnerName}</span></span>) : null} </motion.div> <div className={`grid grid-cols-1 ${hasVideo ? '' : 'lg:grid-cols-5'} gap-8 md:gap-10 items-center`}> <div className={hasVideo ? '' : 'lg:col-span-3'}> {headline ? (<motion.h1 initial={{ opacity: 0, y: 40 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 0.2, duration: 0.9, ease: [0.22, 1, 0.36, 1] }} className={`font-extrabold leading-[0.95] tracking-tight mb-6 ${isPitch ? 'text-5xl sm:text-6xl md:text-8xl lg:text-[5.5rem]' : hasVideo ? 'text-4xl sm:text-5xl md:text-7xl' : 'text-3xl sm:text-4xl md:text-6xl'}`}>{headline}</motion.h1>) : (<motion.h1 initial={{ opacity: 0, y: 40 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 0.2, duration: 0.9, ease: [0.22, 1, 0.36, 1] }} className="text-4xl sm:text-5xl md:text-7xl font-extrabold leading-[0.92] tracking-tight mb-6">Launch premium golf<br />campaigns that create<br /><span className="text-gradient">qualified demand</span></motion.h1>)} <motion.p initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 0.5, duration: 0.7 }} className={`leading-relaxed mb-8 ${isPitch ? 'text-xl md:text-[1.75rem] text-[#d1d5db] font-medium max-w-4xl leading-9' : 'text-base md:text-xl text-[#9ca3af] max-w-3xl'}`}>{subtitle}</motion.p> {isPitch && (<motion.div initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 0.7, duration: 0.5 }} className="mb-8"><p className="text-xs font-mono text-[#4b5563] uppercase tracking-[0.2em] mb-4">Current Partners</p><div className="flex items-center gap-6 md:gap-8 flex-wrap">{pitchPartnerLogos.map((logo) => (<img key={logo.name} src={logo.url} alt={logo.name} className="w-auto object-contain" style={{ filter: 'brightness(0) invert(1)', opacity: 0.5, height: logo.h }} />))}<span className="text-sm font-mono text-[#4b5563]">& more</span></div></motion.div>)} {!isPitch && <div className="mb-8" />} {showPartnerLogo && (<motion.div initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 0.6, duration: 0.5 }} className="mb-8"><img src={partner.partnerLogoUrl} alt={partner.partnerName} className="h-16 md:h-20 w-auto object-contain object-left" style={{ filter: 'brightness(0) invert(1)', opacity: 0.9 }} /></motion.div>)} {partner.isPortfolio && partner.portfolioBrands && partner.portfolioBrands.length > 0 && (<motion.div initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 0.6, duration: 0.5 }} className="mb-8"><div className="hidden sm:block"><PortfolioBracket brands={partner.portfolioBrands} agencyLogoUrl={partner.agencyLogoUrl} agencyName={partner.agencyName} /></div><div className="sm:hidden flex flex-col gap-2"><div className="flex flex-wrap gap-2">{partner.portfolioBrands.map((b) => (b.brandLogoUrl && (<div key={b.brandName} className="flex items-center justify-center h-11 px-4 rounded-lg bg-[#1a1f2e] border border-[#2a3347]"><img src={b.brandLogoUrl} alt={b.brandName} className="h-5 w-auto max-w-[120px] object-contain" style={{ filter: 'brightness(0) invert(1)', opacity: 0.85 }} /></div>)))}</div>{partner.agencyLogoUrl && (<div className="flex items-center gap-2"><div className="w-6 h-px bg-[#2a3347]" /><div className="flex items-center justify-center h-11 px-4 rounded-lg bg-[#1a1f2e]/70 border border-[#2a3347]/70"><img src={partner.agencyLogoUrl} alt={partner.agencyName || 'Agency'} className="h-6 w-auto max-w-[140px] object-contain" style={{ filter: 'brightness(0) invert(1)', opacity: 0.7 }} /></div></div>)}</div></motion.div>)} {showBrandInput && <BrandSpendInput />} {!hasVideo && (<motion.div initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 0.7 }} className="flex flex-wrap items-center gap-3 md:gap-4 mb-10"><button onClick={() => document.getElementById(partner.slug === 'cabot' ? 'the-plan' : 'how-it-works')?.scrollIntoView({ behavior: 'smooth' })} className="inline-flex items-center gap-1.5 text-sm md:text-base font-medium hover:underline transition-colors text-[#00ff9d]"><Zap className="w-4 h-4" /> See How It Works</button></motion.div>)} </div> {!hasVideo && (() => { const cabotHeroImage = 'https://cdn.sanity.io/images/e3wja34v/production/9f686044994dedab4e7c850b1be03c19044da8ce-2064x2736.png'; const isCabot = partner.slug === 'cabot'; const displayImage = isCabot ? cabotHeroImage : images.cobraSweeps; return (<motion.div initial={{ opacity: 0, y: 30, rotate: 1 }} animate={{ opacity: 1, y: 0, rotate: isCabot ? 0 : 2 }} transition={{ delay: 0.5, duration: 1 }} className="lg:col-span-2 hidden md:flex justify-center"><div className="relative"><div className="absolute -inset-10 blur-[80px] opacity-[0.15] rounded-full" style={{ background: 'radial-gradient(circle, #00ff9d, #001a14, transparent)' }} /><img src={displayImage} alt="Campaign creative" className={`relative rounded-[24px] glow-green ${isCabot ? 'w-72 md:w-80 lg:w-[26rem]' : 'w-48 md:w-60 lg:w-68'}`} style={{ filter: 'drop-shadow(0 40px 80px rgba(0,0,0,0.7))' }} />{!isCabot && (<><motion.img src={images.srixonAd1} alt="In-app campaign" className="absolute -left-10 md:-left-14 top-1/3 w-24 md:w-32 rounded-xl shadow-2xl border border-[#2a3347]" animate={{ y: [0, -8, 0] }} transition={{ repeat: Infinity, duration: 4, ease: 'easeInOut' }} style={{ filter: 'drop-shadow(0 20px 40px rgba(0,0,0,0.6))' }} /><motion.div className="absolute -right-4 md:-right-8 bottom-10 bg-[#1a1f2e] border border-[#2a3347] rounded-xl px-3 py-2 shadow-xl" animate={{ y: [0, 6, 0] }} transition={{ repeat: Infinity, duration: 5, ease: 'easeInOut', delay: 1 }}><p className="text-[10px] font-mono text-[#6b7280] uppercase tracking-wider">Qualified Cohort</p><p className="text-lg font-bold text-[#00ff9d]">2,847 golfers</p></motion.div></>)}</div></motion.div>) })()}</div> <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 1.0 }} className="mt-8 grid grid-cols-2 sm:grid-cols-3 md:flex md:flex-wrap gap-6 md:gap-8 lg:gap-12">{defaultKPIs.map((kpi) => (<div key={kpi.label}><span className={`font-bold font-mono text-[#00ff9d] ${isPitch ? 'text-2xl md:text-3xl lg:text-4xl' : 'text-xl md:text-2xl lg:text-3xl'}`}>{kpi.value}</span><span className={`block mt-1 ${isPitch ? 'text-base text-[#6b7280]' : 'text-sm md:text-base text-[#6b7280]'}`}>{kpi.label}</span>{kpi.subtitle && <span className="block text-xs text-[#4b5563] mt-0.5">{kpi.subtitle}</span>}</div>))}</motion.div> {markets && markets.length > 0 && (<motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 1.2 }} className="mt-8"><button onClick={() => setShowMarkets(!showMarkets)} className="inline-flex items-center gap-2 px-4 py-2.5 rounded-xl bg-[#1a1f2e]/80 backdrop-blur-sm border border-[#2a3347] hover:border-[#00ff9d]/40 transition-all group"><Globe className="w-4 h-4 text-[#00ff9d]" /><span className="text-sm md:text-base font-semibold text-white">{marketTitle}</span><span className="text-xs font-mono text-[#6b7280] ml-1">{isCustomMarkets ? 'Apr 2026' : 'As of May 6, 2026'}</span>{showMarkets ? <ChevronUp className="w-4 h-4 text-[#6b7280] group-hover:text-[#00ff9d] transition-colors" /> : <ChevronDown className="w-4 h-4 text-[#6b7280] group-hover:text-[#00ff9d] transition-colors" />}</button>{showMarkets && (<div className="mt-4 bg-[#1a1f2e]/90 backdrop-blur-sm border border-[#2a3347] rounded-xl overflow-hidden max-w-2xl"><div className="divide-y divide-[#2a3347]/60">{markets.map((m) => { const maxUsers = Math.max(...markets.map(x => x.users)); const pct = (m.users / maxUsers) * 100; return (<div key={m.country} className="px-5 py-3 flex items-center gap-4">{m.flag && <span className="text-lg shrink-0">{m.flag}</span>}<span className="text-sm md:text-base text-white font-medium w-32 md:w-40 shrink-0">{m.country}</span><div className="flex-1 h-2 rounded-full bg-[#0f1217] overflow-hidden"><div className="h-full rounded-full" style={{ width: `${pct}%`, background: 'linear-gradient(90deg, #00ff9d, #17A455)' }} /></div><span className="text-sm md:text-base font-mono font-bold text-[#00ff9d] w-16 text-right shrink-0">{m.users.toLocaleString()}</span></div>) })}{othersCount > 0 && (<div className="px-5 py-3 flex items-center gap-4"><span className="text-lg shrink-0">\ud83c\udf0d</span><span className="text-sm md:text-base text-[#9ca3af] font-medium w-32 md:w-40 shrink-0">Others (124+ markets)</span><div className="flex-1 h-2 rounded-full bg-[#0f1217] overflow-hidden"><div className="h-full rounded-full" style={{ width: `${(othersCount / Math.max(...markets.map(x => x.users))) * 100}%`, background: 'linear-gradient(90deg, rgba(0,255,157,0.3), rgba(23,164,85,0.3))' }} /></div><span className="text-sm md:text-base font-mono font-bold text-[#9ca3af] w-16 text-right shrink-0">{othersCount.toLocaleString()}</span></div>)}</div><div className="px-5 py-3 bg-[#0f1217]/90 border-t border-[#2a3347] flex items-center justify-between"><span className="text-sm font-bold text-white">{isCustomMarkets ? 'Total Active in Florida' : 'Total (130+ countries)'}</span><span className="text-lg md:text-xl font-mono font-bold text-[#00ff9d]">{grandTotal.toLocaleString()}{isCustomMarkets ? '' : '+'}</span></div></div>)}</motion.div>)} {isPitch && (<motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 1.4 }} className="mt-12 flex justify-center"><button onClick={() => document.getElementById(partner.slug === 'cabot' ? 'the-plan' : 'case-study')?.scrollIntoView({ behavior: 'smooth' })} className="flex flex-col items-center gap-2 text-[#4b5563] hover:text-[#00ff9d] transition-colors group"><span className="text-sm font-mono uppercase tracking-wider">Start here</span><motion.div animate={{ y: [0, 6, 0] }} transition={{ repeat: Infinity, duration: 2, ease: 'easeInOut' }}><ArrowDown className="w-5 h-5" /></motion.div></button></motion.div>)} </div> </section> ) }
